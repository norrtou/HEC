import { defineConfig } from 'vite';

export default defineConfig({
  // Served from https://norrtou.github.io/HEC/ — assets must resolve under /HEC/.
  // Change to '/' if the site moves to a custom domain root.
  base: '/HEC/',
});
